*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Bequemlichkeits-Kopie der Debug-APK (siehe CLAUDE.md, "Android-Prototyp")
# - Build-Artefakt wie app-debug.apk selbst, deshalb ebenfalls nicht versioniert.
/keinsabbelradio.apk
# Vom Update-Server ausgelieferter Versions-Stempel zu genau dieser APK,
# entsteht bei jedem Build neu (siehe update_server.py/README) - selbes
# Argument wie bei keinsabbelradio.apk.
/version.json
