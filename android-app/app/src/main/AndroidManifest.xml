<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".RadioZapperApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:theme="@style/Theme.RadioZapperMvp"
        android:usesCleartextTraffic="true">

        <!--
            usesCleartextTraffic=true: einige Internetradio-Streams laufen nur ueber
            http:// (kein TLS). Fuer dieses lokale MVP-Prototyp bewusst erlaubt statt
            per Sender eine network_security_config zu pflegen.
        -->

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RadioZapperMvp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".station.StationManagementActivity"
            android:exported="false"
            android:theme="@style/Theme.RadioZapperMvp" />

        <activity
            android:name=".stt.SttSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.RadioZapperMvp" />

        <service
            android:name=".playback.PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!--
            Fuer den Update-Mechanismus (siehe update/UpdateManager.kt): eine
            ueber HTTP heruntergeladene APK-Datei darf seit Android 7 nicht
            mehr per file://-Uri an den PackageInstaller uebergeben werden
            (FileUriExposedException) - FileProvider liefert dafuer eine
            content://-Uri.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
